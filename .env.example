# Application Configuration
NODE_ENV=development
PORT=3000

# Redis Configuration
# Use 'redis' for Docker Compose, 'localhost' for local development
REDIS_HOST=localhost
REDIS_PORT=6379

# Database Configuration
# Leave unset for default ./data/database.db
# For Docker: set to /app/data/database.db
DB_PATH=./data/database.db